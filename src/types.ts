export interface HeaderProps {
  logo: string;
}
export interface ButtonProps {
  name: string;
  disabled: boolean;
}

export interface InputProps {
  placeholder: string;
  disabled: boolean;
  type: 'text' | 'search';
}
